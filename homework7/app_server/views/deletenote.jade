extends layout 

block content
    h1 Delete Note
    p OK to delete #{term}?
    form#formDeleteNote(name="addnote",method="post",action="/deletenote/#{term}")
        button#btnSubmit(type="submit") Delete
